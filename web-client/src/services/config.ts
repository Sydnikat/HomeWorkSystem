const baseUrl = "http://localhost:9000";
export const authServiceUrl = `${baseUrl}/auth`;
export const assignmentServiceUrl = `${baseUrl}/assignment`;
export const groupServiceUrl = `${baseUrl}/group`;
export const homeworkServiceUrl = `${baseUrl}/homework`;
